# values.yaml
replicaCount: 2

image:
  repository: nginx
  tag: "1.25.4"

service:
  port: 80

resources:
  requests:
    cpu: "10m"
    memory: "32Mi"
  limits:
    cpu: "50m"
    memory: "64Mi"

hpa:
  enabled: true
  minReplicas: 2
  maxReplicas: 10
  targetCPUUtilizationPercentage: 50
